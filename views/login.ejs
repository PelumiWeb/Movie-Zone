<!DOCTYPE html>
<html>
<head>
    <title>
        mMovies
    </title>
    <link href="https://fonts.googleapis.com/css2?family=Baloo+Thambi+2&family=Gotudisplay=swap" rel="stylesheet">
  
    <link href="https://fonts.googleapis.com/css2?family=Baloo+Paaji+2:wght@400;500&family=Roboto+Condesed:ital,wght@1,300&display+swap" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Lato:100,300,400,700,900" rel="stylesheet">
   <link rel="stylesheet" href="style.css">

</head>
<body>
    
 <section class="main_section"> 
    <%- include ('partials/nav.ejs') %>
    <form id="Form" class="Form" method="POST">
        <h1 class="Login">Login</h1>
        <div>
            <input id="email"  type="text" class="usernameB" placeholder="Email" name="email" required></p>
            <label for="email" class="label_username">Email</label>
        </div>
        <div>
            <input 
            id="password" type="password"  class="passwordB"
            name="password"
             placeholder="Password"
             required>
            <label for="password" class="label_username">Password</label>
            <!-- <button type="submit" class="submit">Enter</button>  -->
        </div> 
        <div class="Submit_container">  
            <button type="submit" class="submit">Enter</button> 
        </div>
    </form>
    <!-- <script>
        const form = document.querySelector('form')

        form.addEventListener('submit', async (e) => {
            e.preventDefault()

            // get the values
            const email = form.email.value
            const password = form.password.value
            console.log(email, password)

            try {
                const res = await fetch('/login', {
                    method: 'POST',
                    body: JSON.stringify({email, password}),
                    headers: { 'Content-Type': 'application/json' }
                })
                const data = await res.json()
                console.log(data)
            }catch(err) {
                console.log(err)
            }


        })

    </script> -->

</section> 
</body>

</html>   